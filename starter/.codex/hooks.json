{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel)\" && deno run --allow-read --allow-env --allow-run=git scripts/agent-workflow-hook.ts session-start",
            "timeout": 30,
            "statusMessage": "Loading intent-driven workflow guard"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash|apply_patch|Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel)\" && deno run --allow-read --allow-env --allow-run=git scripts/agent-workflow-hook.ts pre-tool-use",
            "timeout": 30,
            "statusMessage": "Checking intent-driven safety rules"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel)\" && deno run --allow-read --allow-env --allow-run=git scripts/agent-workflow-hook.ts stop",
            "timeout": 30,
            "statusMessage": "Checking intent-driven closure"
          }
        ]
      }
    ]
  }
}
